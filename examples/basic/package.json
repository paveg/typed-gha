{
  "name": "example",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "gha build",
    "check": "gha build --check",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@typed-gha/cli": "workspace:*",
    "@typed-gha/core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "2.1.9"
  }
}
