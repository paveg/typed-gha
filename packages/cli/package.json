{
  "name": "@typed-gha/cli",
  "version": "0.0.1",
  "bin": {
    "gha": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json && node scripts/fix-bin-shebang.mjs",
    "prepack": "pnpm run build"
  },
  "dependencies": {
    "@typed-gha/core": "workspace:*",
    "tsx": "^4.19.0",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
